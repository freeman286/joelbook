<% if @users %>
  $('.user-dropdown-menu').html('<%= escape_javascript(render "users/dropdown", :@users => @users)%>');
  $('.user-dropdown-menu').show();
<% else %>
  $('.user-dropdown-menu').hide();
<% end %>