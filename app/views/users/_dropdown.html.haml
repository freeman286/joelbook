-if @users.present?
  -@users.each do |user|
    %li
      .search-item
        =image_tag avatar_url(user, "40"), class: "rounded_corners"
        =link_to user.name, user
-else
  .search-item
    No results