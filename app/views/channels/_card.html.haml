.row
  .col-md-4
    %h4
      =link_to @channel.name, channel_posts_path(@channel)    
  -if @channel.owner_user == current_user
    .col-md-4
      =link_to "Edit details", edit_channel_path(@channel)
      =link_to "Delete channel", @channel, :method => :delete